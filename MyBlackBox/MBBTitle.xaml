<UserControl x:Class="MyBlackBoxCore.MBBTitle"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"  Background="#FFEBEBEB"
             xmlns:sap="http://schemas.microsoft.com/netfx/2009/xaml/activities/presentation" UseLayoutRounding="True" FlowDirection="LeftToRight" Width="665" Height="433.2">
    <Grid Name="pnlMain">
        <Grid.RowDefinitions>
            <RowDefinition Height="34" />
            <RowDefinition Height="200*" />
        </Grid.RowDefinitions>
        <DockPanel>
            <CheckBox Name="chkAtLeastOneWord" DockPanel.Dock="Right" Content="at least one word" IsEnabled="True" HorizontalAlignment="Right" Margin="10,9,3,9" VerticalAlignment="Center" VerticalContentAlignment="Center" />
            <Label Name="lblSepChar" Margin="3" Content="separator" DockPanel.Dock="Right" HorizontalAlignment="Right" VerticalAlignment="Center" Padding="3,0" />
            <TextBox Name="txtSepChar" AllowDrop="True" Margin="0,3,3,3" Width="20" DockPanel.Dock="Right" Text=" " VerticalAlignment="Center" FontSize="14" />
            <TextBox Name="txtTitle" AllowDrop="True" Margin="3" TextChanged="txtTitle_TextChanged" VerticalAlignment="Center" Text="" FontSize="14" HorizontalAlignment="Stretch" VerticalContentAlignment="Center" DockPanel.Dock="Right" />
        </DockPanel>
        <ListView Grid.Row="1" HorizontalAlignment="Stretch" Name="lvTitles" VerticalAlignment="Stretch" 
                  Background="WhiteSmoke" Margin="2"
                  UseLayoutRounding="True" SelectionChanged="lvTitles_SelectionChanged" KeyDown="lvTitles_KeyDown" MouseDoubleClick="lvTitles_MouseDoubleClick" SelectionMode="Single" />
    </Grid>
</UserControl>
